package com.example.servertest;

// Gson은 JSON 문자열을 자바 객체로 바꿔주는데 자바는 객체를 만들 때 클래스를 먼저 정의하는 과정을 거치므로
// JSON 문자열을 자바 객체로 바꿀 때도 클래스를 먼저 정의해야 함
public class MovieList {
    MovieListResult boxOfficeResult; // 받은 응답
}
